import { observer } from 'mobx-react';
import React from 'react';

const EncryptionKeyDetailPage: React.FC = () => {
  return <h1>암호화 키 상세 조회</h1>;
};

export default observer(EncryptionKeyDetailPage);
